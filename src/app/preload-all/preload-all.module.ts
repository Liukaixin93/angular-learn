import { NgModule } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { RouterModule } from '@angular/router';

import { PreloadAllComponent } from './preload-all.component';

@NgModule({
  declarations: [
    PreloadAllComponent,
  ],
  imports: [
    FormsModule,
  ],
  providers: [
    
  ],//放服务的
  bootstrap: [
      
  ],
  entryComponents: [
    
  ]
})
export class PreloadAllModule { }
